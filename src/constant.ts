export const DEFAULT_LIMIT = 6
//Crucial to modify in .env to production domain (including protocol)
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000" // TODO: change to custom .env variable if deploy outside vercl
